{% schema %}
{
  "name": "t:sections.main-product.blocks.share.name",
  "settings": [
    {
      "type": "text",
      "id": "share_label",
      "label": "t:sections.all.text.label"
    }
  ],
  "presets": [{ "name": "t:sections.main-product.blocks.share.name" }]
}
{% endschema %}
