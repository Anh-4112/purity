{% schema %}
{
  "name": "t:sections.all.product_setting.share.name",
  "settings": [
    {
      "type": "text",
      "id": "share_label",
      "label": "t:sections.all.text.label"
    }
  ],
  "presets": [{ "name": "t:sections.all.product_setting.share.name" }]
}
{% endschema %}
