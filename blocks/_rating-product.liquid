{% render 'review', review_star: true, product: closest.product %}
{% schema %}
{
  "name": "t:sections.all.product_setting.rating.name",
  "class": "block-product__rating mb-25",
  "presets": [{ "name": "t:sections.all.product_setting.rating.name" }]
}
{% endschema %}
