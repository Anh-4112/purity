{% render 'product-badges', card_product: closest.product, type: 'price_badges' %}
{% schema %}
{
  "name": "t:sections.all.product_setting.badges.name",
  "class": "block-product__badges mb-10",
  "presets": [{ "name": "t:sections.all.product_setting.badges.name" }]
}
{% endschema %}
