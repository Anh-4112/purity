{% render 'product-badges', card_product: closest.product.selected_or_first_available_variant, type: 'price_badges' %}
{% schema %}
{
  "name": "t:sections.all.product_setting.badges.name",
  "class": "block-product__badges mb-10 slideIn-animate",
  "presets": [{ "name": "t:sections.all.product_setting.badges.name" }]
}
{% endschema %}
