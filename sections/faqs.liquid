{%- liquid
  assign section_st = section.settings
  assign heading = section_st.heading
  assign subheading = section_st.subheading
  assign show_faqs_message = section_st.show_faqs_message
  assign desktop_content_position = section_st.desktop_content_position
  assign description = section_st.description
  assign button_label = section_st.button_label
  assign button_link = section_st.button_link

  if section_st.section_width == 'fix_width'
    assign section_width = 'container'
  elsif section_st.section_width == 'full_width'
    assign section_width = 'full-width px-20'
  else
    assign section_width = 'fluid_container'
  endif
-%}
{%- capture style -%}
  --section-pt: {{ section_st.padding_top }}; --section-pb: {{ section_st.padding_bottom }};
{%- endcapture -%}
<section
  class="section section__faq color-{{ section_st.color_scheme }} gradient"
  style="{{ style | strip | strip_newlines }}"
>
  <div class="{{ section_width }}">
    <div
      class="section__faqs-content{% if show_faqs_message %} flex flex-wrap{% endif %}{% if desktop_content_position == 'right' %} flex-1025-row-reverse{% endif %}"
    >
      {% if show_faqs_message %}
        <div class="faqs__info">
          {%- if subheading != blank -%}
            <div class="faqs__subheading">{{ subheading }}</div>
          {%- endif -%}
          {%- if heading != blank -%}
            <div class="faqs__heading">{{ heading }}</div>
          {%- endif -%}
          {%- if description != blank -%}
            <div class="faqs__description">{{ description }}</div>
          {%- endif -%}
          {%- if button_label != blank -%}
            <a
              {% if button_link == blank %}
                role="link" aria-disabled="true"
              {% else %}
                href="{{ button_link | default: "#" }}"
              {% endif %}
              aria-label="{{ button_label }}"
              class="w-full"
            >
              {{ button_label }}
            </a>
          {%- endif -%}
        </div>
      {% endif %}
      <div class="{% if show_faqs_message %}w-full{% endif %}">
        {% content_for 'blocks' %}
      </div>
    </div>
  </div>
</section>
{% schema %}
{
  "name": "t:sections.faq.name",
  "tag": "section",
  "class": "section",
  "disabled_on": {
    "groups": ["header", "footer", "custom.overlay"]
  },
  "blocks": [
    {
      "type": "@app"
    },
    {
      "type": "_item-faq"
    }
  ],
  "settings": [
    {
      "type": "header",
      "content": "t:sections.all.general"
    },
    {
      "type": "select",
      "id": "section_width",
      "options": [
        {
          "value": "fix_width",
          "label": "t:sections.all.layout.fix_width"
        },
        {
          "value": "stretch_width",
          "label": "t:sections.all.layout.stretch_width"
        },
        {
          "value": "full_width",
          "label": "t:sections.all.layout.full_width"
        }
      ],
      "default": "fix_width",
      "label": "t:sections.all.layout.label"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:sections.all.colors.label",
      "info": "t:sections.all.colors.info",
      "default": "default-color-scheme"
    },
    {
      "type": "checkbox",
      "id": "open_all_question",
      "label": "t:sections.faq.settings.open_all_question",
      "default": false
    },
    {
      "type": "header",
      "content": "t:sections.faq.heading.content"
    },
    {
      "type": "checkbox",
      "id": "show_faqs_message",
      "label": "t:sections.faq.settings.show_faqs_message",
      "default": true
    },
    {
      "type": "select",
      "id": "desktop_content_position",
      "options": [
        {
          "value": "left",
          "label": "t:sections.all.image.desktop_position.left"
        },
        {
          "value": "right",
          "label": "t:sections.all.image.desktop_position.right"
        }
      ],
      "default": "left",
      "label": "t:sections.all.desktop_content_position.label",
      "info": "t:sections.all.desktop_content_position.info_2"
    },
    {
      "type": "text",
      "id": "subheading",
      "label": "t:sections.all.content_setting.subheading.label",
      "default": "Have a Question?"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "t:sections.all.content_setting.heading.label",
      "default": "Help Center!"
    },
    {
      "type": "richtext",
      "id": "description",
      "label": "t:sections.all.content_setting.description.label",
      "default": "<p>Add a short description for this block.</p>"
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "t:sections.all.content_setting.button_label.label",
      "info": "t:sections.all.content_setting.button_label.info",
      "default": "Button label"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "t:sections.all.content_setting.link.label"
    },
    {
      "type": "header",
      "content": "t:sections.all.padding.section_padding_heading"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "label": "t:sections.all.padding.padding_top",
      "default": 48
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "label": "t:sections.all.padding.padding_bottom",
      "default": 48
    }
  ],
  "presets": [
    {
      "name": "t:sections.faq.name",
      "blocks": [
        {
          "type": "_item-faq"
        },
        {
          "type": "_item-faq"
        },
        {
          "type": "_item-faq"
        }
      ]
    }
  ]
}
{% endschema %}
